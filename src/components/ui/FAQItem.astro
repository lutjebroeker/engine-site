---
interface Props {
  question: string;
  answer: string;
}

const { question, answer } = Astro.props;
---

<details class="border-b border-void-border group">
  <summary class="flex items-center justify-between py-5 cursor-pointer list-none font-sans font-medium text-off-white text-base hover:text-emerald transition-colors">
    <span>{question}</span>
    <span class="ml-4 flex-shrink-0 text-text-dim group-open:rotate-45 transition-transform duration-200">
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M8 3v10M3 8h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
    </span>
  </summary>
  <div class="pb-5 text-text-muted font-sans text-base leading-relaxed">
    {answer}
  </div>
</details>
